# Docker Secrets Configuration
# This file contains example secret configurations
# In production, use Docker secrets or external secret management

# MinIO Secrets
MINIO_ROOT_USER_FILE=/run/secrets/minio_root_user
MINIO_ROOT_PASSWORD_FILE=/run/secrets/minio_root_password

# PostgreSQL Secrets
POSTGRES_DB=metastore
POSTGRES_USER_FILE=/run/secrets/postgres_user
POSTGRES_PASSWORD_FILE=/run/secrets/postgres_password

# Hive Metastore Secrets
HIVE_METASTORE_USER_FILE=/run/secrets/hive_user
HIVE_METASTORE_PASSWORD_FILE=/run/secrets/hive_password

# Trino Secrets
TRINO_PASSWORD_FILE=/run/secrets/trino_password

# Airflow Secrets
AIRFLOW_DATABASE_PASSWORD_FILE=/run/secrets/airflow_db_password
AIRFLOW_WEBSERVER_PASSWORD_FILE=/run/secrets/airflow_web_password